{
  "name": "smart-irrigation-orange-pi-ota",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Smart irrigation web app and authenticated Orange Pi to ESP32-S3 OTA bridge",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check script.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "firebase-admin": "^14.2.0",
    "helmet": "^8.3.0"
  }
}
